using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SwitchToCanvasCamera : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Camera camOnThis = GetComponent<Camera>();
        //camOnThis.enabled = true;
    }

}
